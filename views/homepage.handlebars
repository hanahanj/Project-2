{{!-- {{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{{get_emoji}}} {{user.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}} --}}


{{!-- <div class="card">
  <h5 class="card-title"> {{> playlist-name}}</h5>
 
  <div class="card-body">
 {{> tag}}
  </div>
</div> --}}

{{#each playlists as |playlist| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{playlist_name}} {{tag}}</h2>
  </div>

  <div class="col-md-6">
    <h2>{{created_at}} {{updated_at}}</h2>
  </div>
  
</div>
{{/each}}



{{!-- {{#each playlists as |playlist|}}
{{{playlist_name}}}
{{{tag}}}
{{{created_at}}}
{{{updated_at}}} --}}

{{/each}}
